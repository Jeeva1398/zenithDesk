{
  "name": "zenithdesk-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end and API-level tests for the ZenithDesk main app",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test --project=api",
    "test:ui": "playwright test --project=browser",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "dotenv": "^17.4.2",
    "mysql2": "^3.23.3"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.3"
  }
}
